<Window x:Class="WpfApp.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="WPF" Height="450" Width="800"
        ResizeMode="CanResize"
        >
    <Grid>
        <!-- Simple Property -->
        <!-- Type Converter -->
        <!--<Button Name="btn"  Width="100" Height="50"  Foreground="White" FontSize="26" FontWeight="Thin">
            Save
        </Button>-->

        <!-- Complex Property -->
        
        <Button Name="btn"  Width="100" Height="50"  >
            <Button.Content>
                <Image Source="saberJ.jpg"></Image>
            </Button.Content>
            <Button.FontSize>
                22
            </Button.FontSize>
            <Button.Foreground>
                white
            </Button.Foreground>
            <Button.Background>
                black
            </Button.Background>
        </Button>


    </Grid>
</Window>
